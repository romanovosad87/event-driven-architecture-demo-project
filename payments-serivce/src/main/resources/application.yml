spring:
  application:
    name: payments-serivce
  datasource:
    password: admin
    username: admin
    url: jdbc:h2:file:~/payments;AUTO_SERVER=true
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

server:
  port: 0
